# Contract Addresses
pools:
  - address: "0000000000000000000000000000000000001017"  # Pool contract address (ETHST-USDST pool)
    external_token_name: "ETH"
  - address: "0000000000000000000000000000000000001019"  # Pool contract address (WBTC-USDST pool)
    external_token_name: "BTC"

# Trading Parameters  
trading:
  fee_bps: 30  # Pool fee in basis points (0.3%)
  min_profit: 0.01  # $0.01 minimum profit

# Oracle Configuration  
oracle:
  timeout: 10  # API request timeout in seconds (unused, oracle is hardcoded)

# Execution Settings
execution:
  execution_interval: 60  # seconds between scans
